---
description: Megatron-LM 项目规则 - 禁止生成不必要的文档和测试文件
globs:
alwaysApply: true
---

# Megatron-LM 项目规则

## 文件生成限制

### 禁止生成的文件类型
- **禁止生成 Markdown 文档文件** (*.md, *.MD, README.md, CHANGELOG.md 等)，除非用户明确要求
- **禁止生成总结说明文件** (SUMMARY.md, NOTES.md, DOCUMENTATION.md 等)
- **禁止生成测试脚本** (test_*.py, test_*.sh, *_test.py 等，除非用户明确要求)
- **禁止生成示例/演示文件** (example_*.py, demo_*.py, sample_*.py 等，除非用户明确要求)
- **禁止生成配置文件** (config_*.yaml, config_*.json 等，除非用户明确要求)
- **禁止生成日志文件** (*.log, *.txt 等临时文件)
- **禁止生成模拟/仿真文件** (simulation, simluation 等)

### 允许生成的文件类型
- 核心业务代码文件 (*.py, *.cpp, *.cu, *.h 等)
- 用户明确要求创建的文件
- 修复 bug 或实现功能所需的必要文件

## 代码生成原则

### 代码修改
- **优先修改现有文件**，而不是创建新文件
- 只在必要时创建新文件（用户明确要求或功能需要）
- 不要创建"辅助"或"说明"性质的临时文件

### 代码注释
- 代码注释应该简洁明了，使用中文或英文均可
- 不要在代码中创建过长的文档字符串，除非是 API 文档
- 避免在代码中插入"TODO"或"FIXME"注释，除非是修复 bug 的一部分

### 响应格式
- 直接提供代码修改，不要生成额外的说明文档
- 如果需要解释，在对话中直接说明，不要创建文件
- 代码修改应该完整且可直接使用
- 对话采用中文

## 会话上下文

### 上下文链接
- 每个会话应该能够访问和理解项目的整体结构
- 参考现有代码风格和架构模式
- 保持与项目现有代码的一致性

### 代码理解
- 在修改代码前，先理解相关文件的上下文
- 使用 codebase_search 和 grep 工具查找相关代码
- 确保修改不会破坏现有功能

## 特殊规则

### Megatron-LM 项目特定规则
- 这是一个大型语言模型训练框架项目
- 修改时要注意分布式训练、管道并行等特性
- 不要修改核心训练逻辑，除非用户明确要求
- 保持与现有代码风格一致（PEP 8 for Python）

### 错误处理
- 如果生成的文件不符合要求，应该立即删除
- 如果误创建了不需要的文件，应该告知用户并删除

## 用户交互

### 明确需求
- 如果用户的需求不明确，应该询问而不是猜测
- 在创建新文件前，确认用户是否真的需要
- 优先使用现有代码和工具，而不是重新实现

### 文件操作
- 删除文件前应该确认
- 修改重要文件前应该先读取和理解
- 保持 git 仓库的整洁，不要提交临时文件

## 总结

**核心原则：只生成必要的代码文件，不生成文档、测试、示例等辅助文件，除非用户明确要求。**